# This file is updated automatically by the release workflow.
class PixelAgentsTui < Formula
  desc "Terminal UI visualizing Claude Code agents as animated pixel characters"
  homepage "https://github.com/esumerfd/pixel-agents-tui"
  version "{{VER}}"

  on_macos do
    on_arm do
      url "https://github.com/esumerfd/pixel-agents-tui/releases/download/{{TAG}}/pixel-agents-tui-{{TAG}}-aarch64-apple-darwin.tar.gz"
      sha256 "{{SHA_ARM64}}"
    end
    on_intel do
      url "https://github.com/esumerfd/pixel-agents-tui/releases/download/{{TAG}}/pixel-agents-tui-{{TAG}}-x86_64-apple-darwin.tar.gz"
      sha256 "{{SHA_X86}}"
    end
  end

  on_linux do
    url "https://github.com/esumerfd/pixel-agents-tui/releases/download/{{TAG}}/pixel-agents-tui-{{TAG}}-x86_64-unknown-linux-gnu.tar.gz"
    sha256 "{{SHA_LINUX}}"
  end

  def install
    bin.install "pixel-agents-tui"
  end

  test do
    system "#{bin}/pixel-agents-tui", "--version"
  end
end
